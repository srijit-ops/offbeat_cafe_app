import { Button } from "react-native"

const CustomTextComponent = (props)=>{
    return (
        <Button title={props.title} style={{backgroundColor:"pink"}}/>
   )
}
export default CustomTextComponent